# Generated by main.py. Do not edit manually.
name: yui-relayer-module

services:
  yui-relayer:
    image: "yui-relayer-local:dev"
    build:
      context: "."
      dockerfile: Dockerfile
    container_name: "yui-relayer"
    entrypoint: ["sleep"]
    command: ["infinity"]
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - type: bind
        source: "./runtime"
        target: /root/.yui-relayer
    networks:
      interoperability:
        ipv4_address: "172.30.0.20"
    healthcheck:
      test: ["CMD-SHELL", "test -x /usr/local/bin/yrly"]
      interval: 5s
      timeout: 3s
      retries: 10

networks:
  interoperability:
    name: "interoperability_network"
    external: true
